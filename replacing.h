#ifndef REPLACING_H
#define REPLACING_H

#include <string>

namespace encryption_methods {
  struct replacing {
    int key;
    std::string first_str;
    std::string second_str;
  };
} // namespace encryption_methods

#endif // REPLACING_H
